# v10.22 — actualización sobre v10.21

Suba el contenido de este ZIP conservando la estructura de carpetas y reemplazando los archivos coincidentes.

No contiene `wrangler.jsonc`, por lo que no sobrescribe el binding D1, variables ni Secrets de producción.

La actualización incorpora la migración `0009_remove_seeded_operational_defaults.sql`; el proceso normal de deploy debe aplicarla antes de publicar el Worker.

Verifique después del despliegue `/api/version`: debe indicar `v10.22 / 1.0.32-d1-web`.
