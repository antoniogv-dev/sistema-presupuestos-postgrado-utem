import { z, ZodError } from "zod";import { apiError,hasAccess,requireApiIdentity } from "@/lib/auth/api-access";import { d1Id,d1Json,runD1Batch } from "@/lib/database/d1-atomic";import { getPrismaClient } from "@/lib/database/prisma";import { d1Database } from "@/lib/runtime-env";type Context={params:Promise<{planId:string}>};
const schema=z.object({name:z.string().trim().min(3).max(240),normalizeStaff:z.boolean(),allocationMethod:z.enum(["EQUAL","STUDENTS","EQUIVALENT_ENROLLMENTS","MANUAL"]),notes:z.string().trim().max(2000).optional().nullable(),intakes:z.array(z.object({budgetId:z.string().min(1),windowId:z.string().optional().nullable(),label:z.string().trim().min(1).max(160),position:z.number().int().min(0).max(100),allocationRate:z.number().min(0).max(1).default(0)})).min(1).max(24),sharedCosts:z.array(z.object({year:z.number().int().min(2000).max(2100),category:z.string().trim().min(1).max(120),name:z.string().trim().min(1).max(240),amount:z.number().int().nonnegative(),allocationMethod:z.enum(["INHERIT","EQUAL","STUDENTS","EQUIVALENT_ENROLLMENTS","MANUAL"]).default("INHERIT"),note:z.string().trim().max(600).optional().nullable()})).max(100)});const json=(v:unknown)=>JSON.parse(JSON.stringify(v,(_,x)=>typeof x==="bigint"?x.toString():x));
export async function PUT(request:Request,context:Context){try{const identity=await requireApiIdentity(request);if(!hasAccess(identity,"GESTOR"))throw new Error("FORBIDDEN");const{planId}=await context.params,input=schema.parse(await request.json()),prisma=getPrismaClient();const plan=await prisma.annualOfferingPlan.findUnique({where:{id:planId},include:{program:{include:{intakeWindows:true}}}});if(!plan)throw new Error("NOT_FOUND");if(plan.program.type!=="MAGISTER_PROFESIONAL")throw new Error("ANNUAL_PLAN_PROFESSIONAL_ONLY");if(input.intakes.length>plan.program.maxAnnualIntakes)throw new Error("ANNUAL_PLAN_MAX_INTAKES");const ids=[...new Set(input.intakes.map(i=>i.budgetId))];const bs=await prisma.cohortBudget.findMany({where:{id:{in:ids},deletedAt:null},select:{id:true,programId:true}});if(ids.length!==input.intakes.length||bs.length!==ids.length||bs.some(b=>b.programId!==plan.programId))throw new Error("ANNUAL_PLAN_BUDGET_MISMATCH");const db=d1Database();const st:D1PreparedStatement[]=[db.prepare(`UPDATE "AnnualOfferingPlan" SET "name"=?,"normalizeStaff"=?,"allocationMethod"=?,"notes"=?,"updatedAt"=CURRENT_TIMESTAMP WHERE "id"=?`).bind(input.name,input.normalizeStaff?1:0,input.allocationMethod,input.notes??null,planId),db.prepare(`DELETE FROM "AnnualOfferingPlanIntake" WHERE "planId"=?`).bind(planId),db.prepare(`DELETE FROM "AnnualOfferingPlanSharedCost" WHERE "planId"=?`).bind(planId)];for(const i of input.intakes)st.push(db.prepare(`INSERT INTO "AnnualOfferingPlanIntake" ("id","planId","budgetId","windowId","label","position","allocationRate","createdAt") VALUES (?,?,?,?,?,?,?,CURRENT_TIMESTAMP)`).bind(d1Id("annual-intake"),planId,i.budgetId,i.windowId??null,i.label,i.position,i.allocationRate));for(const c of input.sharedCosts)st.push(db.prepare(`INSERT INTO "AnnualOfferingPlanSharedCost" ("id","planId","year","category","name","amount","allocationMethod","note","createdAt","updatedAt") VALUES (?,?,?,?,?,?,?,?,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)`).bind(d1Id("annual-cost"),planId,c.year,c.category,c.name,c.amount,c.allocationMethod,c.note??null));st.push(db.prepare(`INSERT INTO "AuditLog" ("id","userId","entity","entityId","action","newValue","createdAt") VALUES (?,?,'AnnualOfferingPlan',?,'UPDATE_ANNUAL_PLAN',?,CURRENT_TIMESTAMP)`).bind(d1Id("audit"),identity.userId,planId,d1Json(input)));await runD1Batch(st);return Response.json(json(await prisma.annualOfferingPlan.findUnique({where:{id:planId},include:{program:{include:{intakeWindows:true}},intakes:{include:{window:true,budget:true},orderBy:{position:"asc"}},sharedCosts:true}})))}catch(e){if(e instanceof ZodError)return Response.json({error:"Datos del plan anual inválidos.",issues:e.issues},{status:400});return apiError(e)}}
