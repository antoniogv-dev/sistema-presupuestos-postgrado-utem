# Verificación v11.0.10 — Etiquetas exactas XLSX

La versión `1.1.10-d1-web` debe cumplir:

1. C14 = `matrículas equivalentes`.
2. C15 = `estudiantes`.
3. No contiene `(fórmula)` ni `(redondeo fórmula)` en esas etiquetas.
4. B14 conserva la fórmula de punto de equilibrio.
5. B15 conserva `ROUNDUP(B14,0)`.
6. No requiere migraciones D1 adicionales.
