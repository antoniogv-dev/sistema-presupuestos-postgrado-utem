# Verificación v10.24

Controles previstos:

1. Un programa recién creado y activo aparece en el selector superior de Presupuestos tras “Actualizar listas”.
2. Un programa sin presupuesto no mantiene visible la cohorte del programa anterior.
3. Crear “Nuevo presupuesto” utiliza el `programFilterId` seleccionado.
4. Los presupuestos existentes siguen sin poder reasignar `programId`.
5. La selección de cohorte se filtra por programa.
6. La UI utiliza la paleta azul + negro `#000000` + mostaza `#FFB344` + beige `#FFF8E5`.
7. No se agregan migraciones D1.
